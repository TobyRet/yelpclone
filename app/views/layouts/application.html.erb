<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Yelp Clone | Makers Academy" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="fixed">
      <nav class="top-bar nav_inner" data-topbar>
        <div id = "logo">Yelp Clone</div>
      </nav>
    </div>
    <div class = "content_wrapper">
      <%= flash[:notice] %>
      <%= yield %>
      <%= javascript_include_tag "application" %>
    </div>
  </body>
</html>